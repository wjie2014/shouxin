# 验收说明

## 本地访问

- 前端：`http://localhost:5173/`
- 后端健康：`http://localhost:3000/actuator/health`
- 默认管理员：`admin / admin123`
- 提交者：`submitter1 / admin123`
- 一级审核：`reviewer1 / admin123`
- 二级审核：`reviewer2 / admin123`
- 三级审核：`reviewer3 / admin123`
- 问答管理员：`qaadmin / admin123`

所有初始账号首次登录均应修改密码，且支持管理员重置。

## 已覆盖的核心验收路径

1. 新建问答对支持三级目录、默认字段方案动态表单、问题/答案富文本、图片和表格、附件拖拽上传、保存草稿和提交审核。
2. 审核流程按一级、二级、三级指定人员依次流转；非当前审核人被拒绝；支持任一通过/全部通过、驳回意见、修改后从一级重提和完整历史记录。
3. 三级审核通过自动发布；已发布内容可发起更新、保留旧版本、通过后切换新版本；支持退役和退役后重建版本审核。
4. 问答列表支持三级目录、状态、日期、关键词筛选，排序、分页、批量提交/删除/导出及五页签详情。
5. 第一、第二阶段 Excel 支持模板下载、预览校验、确认导入、多 Sheet；导出文件有完整边框且空值不会显示 `null`。
6. 字段方案支持新建、编辑、复制、删除、默认、导入导出、13 种字段、选项校验、列配置、移动排序和表单预览。
7. 目录支持三级树、新增、编辑、删除、上下移动、折叠、批量导入导出；预置 11 个一级目录和子目录数据。
8. 工作台和运营仪表盘使用 ECharts 展示指标、状态/领域/单位分布、30 天趋势、审核通过率和版本统计，支持悬浮提示。
9. 自定义分析覆盖完整筛选、8 个指标、趋势/分布/交叉/效率/漏斗/超期/审核人排名/行为反馈、下钻、结果分页排序、同比环比、Excel、方案 CRUD、公共模板、订阅和报告中心。
10. 系统管理覆盖用户、角色权限树、操作日志和系统参数 CRUD；后端按权限码实际鉴权。
11. 多页签保持挂载，并持久化主要业务页的筛选、分页和排序状态；关闭页签清理对应状态。
12. 生产部署使用 React + Nginx、Java Spring Boot、达梦 DM8、Docker Compose，支持无源码镜像交付。

## 已执行自动验证

- `pnpm --dir apps/web build`：严格 TypeScript 检查和生产构建通过。
- `mvn -f backend/pom.xml test`：Spring Boot 后端测试通过。
- `bash scripts/smoke.sh`：登录、用户信息、目录、仪表盘、列表、审核、流程、字段方案、日志、配置和两类 Excel 导出通过。
- 自定义分析 8 种模式均完成真实 HTTP 查询；明细分页和 OOXML Excel 导出通过。
- 第二阶段原始 Excel：12 个 Sheet 中 11 个业务 Sheet 可识别，11/11 数据行有效；说明 Sheet 自动跳过。
- 导出 Excel：12 个 Sheet、单元格边框完整、无字面量 `null`；数据权限隔离有效。
- 附件上传、列表、下载字节一致性、删除全链路通过。
- 指定审核人三级流转、越权 403、驳回必填、驳回重提均通过。

## 手工验收重点

使用 1366×768、1920×1080 分辨率及 Chrome/Edge/Firefox 最新版本，逐菜单核对视觉、键盘焦点、长文本、空状态和弹窗滚动。生产服务器需另行执行 50 并发、10 万数据量、1 万行导出、备份恢复、HTTPS 和安全扫描验收。
