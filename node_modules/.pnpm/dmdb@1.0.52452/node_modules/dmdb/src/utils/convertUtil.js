"use strict";

Object.defineProperty(exports, "__esModule", {
    value: !0
});

var stringUtil_1 = require("./stringUtil"), error_1 = require("../driver/error"), ConvertUtil = function() {
    function r() {}
    return r.toBoolean = function(r) {
        return "0" !== (r = r.trim()) && ("1" === r || stringUtil_1.StringUtil.equalsIgnoreCase(r, "true"));
    }, r.toBigInt = function(r) {
        r = r.trim();
        var t = BigInt(0), e = r.indexOf(".");
        return 0 <= e && (e <= r.length - 1 && "5" <= r.charAt(e + 1) && r.charAt(e + 1) <= "9" && (t = "-" === r.charAt(0) ? BigInt(-1) : BigInt(1)), 
        r = r.substring(0, e)), BigInt(r) + t;
    }, r.numberToPlainString = function(r) {
        if (Number.isNaN(r) || !Number.isFinite(r)) throw error_1.DBError.ECJS_DATA_CONVERTION_ERROR();
        var t = r.toString();
        return t.includes("e") ? t.replace(/^(-?)(\d+)\.?(\d*)e([+-]\d+)$/, function(r, t, e, n, i) {
            var u = Number(i);
            return u < 0 ? t + "0." + "0".repeat(-u - 1) + e + n : n.length <= u ? t + e + n + "0".repeat(u - n.length) : t + e + n.slice(0, u) + "." + n.slice(u);
        }) : t;
    }, r;
}();

exports.ConvertUtil = ConvertUtil;