"use strict";

Object.defineProperty(exports, "__esModule", {
    value: !0
});

var dateUtil_1 = require("./dateUtil"), StringUtil = function() {
    function n() {}
    return n.equalsIgnoreCase = function(r, n) {
        return r && n ? r.toLowerCase() === n.toLowerCase() : r === n;
    }, n.isDigit = function(r) {
        return /(^[0-9]+$)/.test(r);
    }, n.isUnicodeLetter = function(r) {
        return /^\p{L}/u.test(r);
    }, n.isLetterOrDigit = function(r) {
        return n.isDigit(r) || n.isUnicodeLetter(r);
    }, n.hexStringToBuffer = function(r) {
        var n = r.trim();
        if (0 !== n.indexOf("0x") && 0 !== n.indexOf("0X") || (n = n.substring(2)), 0 === n.length) return Buffer.alloc(0);
        n.length % 2 != 0 && (n = "0" + n);
        for (var t = !1, e = Buffer.allocUnsafe(n.length / 2), u = 0, f = 0; f < n.length; f += 2) {
            var i = this.convertHex(n.charCodeAt(f)), a = this.convertHex(n.charCodeAt(f + 1));
            if (-1 === i || -1 === a) {
                t = !0;
                break;
            }
            e[u++] = 16 * i + a;
        }
        return t && (e = Buffer.from(r)), e;
    }, n.isHighSurrogate = function(r) {
        return 55296 <= r && r <= 56319;
    }, n.charCount = function(r) {
        return 65536 <= r ? 2 : 1;
    }, n.processDoubleQuoteOfName = function(r) {
        return n.processQuoteOfName(r, '"');
    }, n.processSingleQuoteOfName = function(r) {
        return n.processQuoteOfName(r, "'");
    }, n.processQuoteOfName = function(r, n) {
        return n && r ? r.split(n).join(n + n) : r;
    }, n.array2String = function(r) {
        return r.map(function(r) {
            return n.escape(r);
        }).join(", ");
    }, n.translate = function(r) {
        if (-1 === r.indexOf("\\")) return r;
        for (var n = [], t = 0; t < r.length; t++) {
            var e = r.charAt(t);
            if ("\\" !== e) n.push(e); else {
                var u = r.charAt(t + 1);
                switch (u) {
                  case "b":
                    n.push("\b");
                    break;

                  case "f":
                    n.push("\f");
                    break;

                  case "n":
                    n.push("\n");
                    break;

                  case "r":
                    n.push("\r");
                    break;

                  case "t":
                    n.push("\t");
                    break;

                  default:
                    n.push(u);
                }
                t++;
            }
        }
        return n.join("");
    }, n.escape = function(r) {
        if (null == r) return "NULL";
        switch (typeof r) {
          case "boolean":
            return r ? "1" : "0";

          case "number":
            return r.toString();

          case "object":
            if (r instanceof Date) return "'" + dateUtil_1.DateUtil.formatDate(r, !0) + "'";
            if (r instanceof Buffer) return "0x" + r.toString("hex");

          default:
            return "'" + n.processSingleQuoteOfName(r.toString()) + "'";
        }
    }, n.convertHex = function(r) {
        return r >= "0".charCodeAt(0) && r <= "9".charCodeAt(0) ? r - "0".charCodeAt(0) : r >= "a".charCodeAt(0) && r <= "f".charCodeAt(0) ? r - "a".charCodeAt(0) + 10 : r >= "A".charCodeAt(0) && r <= "F".charCodeAt(0) ? r - "A".charCodeAt(0) + 10 : -1;
    }, n;
}();

exports.StringUtil = StringUtil;